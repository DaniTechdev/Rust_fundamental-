fn main(){
    //impl From<bool> for i32

//     let i1:i32 = false.into();//0
//     let i2:i32 = i32::from(false);

//     assert_eq!(i1,i2);
//     assert_eq!(i1,0);

//     //Fix the error in two ways
//     //1.impl From<char> for u32 . maybe you should check the docs mentioned above to
//     //2. a key from the last chapter

//     let i3: u32 = 'a'.into();


//     //Fix the error into two ways


//     // let s:String = String::from('a');
//    //or
//     let s:String = 'a'.into();

//     println!("success");




//From is now in `std::prelude`, so there is no need to introduce it into
//use std::convert::From



}